# College Hub - Full Stack Integration

## ✅ Готово!

Бэкенд создан и интегрирован с фронтендом. Теперь все данные хранятся в SQLite базе, а не в localStorage.

## 🚀 Запуск

### 1. Запустить Backend
```powershell
cd backend
npm start
```
Сервер: `http://localhost:3000`

### 2. Запустить Frontend (в новом терминале)
```powershell
npm run dev
```
Приложение: `http://localhost:5173`

## 🔑 Вход

- **Student ID**: `12345`
- **Password**: `12345`

Или создайте новый аккаунт - он автоматически создастся в базе.

## 📊 Что работает через API

✅ **Аутентификация**: JWT токены, хэширование паролей
✅ **Новости**: Лайки, комментарии (добавить/редактировать/удалить)
✅ **Клубы**: Создание, присоединение, выход (с соц.сетями)
✅ **Расписание**: CRUD операции с датами
✅ **Проекты**: Создание и просмотр
✅ **Профиль**: Обновление данных пользователя

## 🗄️ База данных

SQLite база создаётся автоматически в `backend/database.sqlite` с начальными данными (новости, клубы, расписание).

Чтобы сбросить базу - удалите файл и перезапустите backend.

## 🔧 Конфигурация

- `backend/.env` - Backend настройки (PORT, JWT_SECRET)
- `.env` - Frontend настройки (VITE_API_URL)

## 📝 API Documentation

Все endpoints описаны в `backend/README.md`
